/* const person={
  name:"andrew",
  age:26,
  location:{
  city:"philadelfia",
  temp:88
  }
}
const { name='anonymous', age}=person;
console.log(`${name} is ${age}`);
const {city, temp:temperature}=person.location;

console.log(`its from ${city} has ${temperature}`); */

/* const book={
  title:"ego is enemy",
  author:"Rayan holiday",
  publisher:{
    name:"penguin"
  }
}

const {name:publishername='self_publisher'}=book.publisher
console.log(publishername); */
/* 
const item=[, '$2.00',' $2.50', '$2.75']

const [coffee="coffee (iced)",, Mcost]=item
console.log(`a medium ${coffee} costs ${Mcost}`) */